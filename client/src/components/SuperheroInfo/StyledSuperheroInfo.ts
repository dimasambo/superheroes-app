import styled from 'styled-components';

export const StyledSuperheroInfo = styled.div`
  .infoBox {
    margin: 10px 0;
  }

  .titleInfo {
    font-weight: bold;
  }

  a {
    color: inherit;
    text-decoration: none;
  }
`